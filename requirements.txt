# Dépendances de Didacode
# Installation : pip install -r requirements.txt
#
# WeasyPrint s'appuie sur des bibliothèques système (Pango et HarfBuzz)
# qui ne s'installent pas par pip. Sous Debian/Ubuntu :
#     sudo apt install libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz0b libharfbuzz-subset0
# Sous macOS (Homebrew) :
#     brew install pango
# Détails : https://doc.courtbouillon.org/weasyprint/stable/first_steps.html

# Conversion HTML -> PDF
weasyprint>=60

# Coloration syntaxique du code
pygments>=2.15

# Conversion Markdown -> HTML
markdown>=3.4

# Lecture du fichier de configuration (option --config uniquement)
pyyaml>=6.0

# Versions utilisées lors des derniers tests :
#   weasyprint 70.0 · pygments 2.20.0 · markdown 3.10.2 · pyyaml 6.0.3
#
# Les tests se lancent sans dépendance supplémentaire :
#     python -m unittest discover -s tests
